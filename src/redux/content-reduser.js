// const INCREASE_VALUE = 'INCREASE_VALUE';
// const DECREASE_VALUE = 'DECREASE_VALUE';

// const initialState = {
//   number: 10,
//   mutableValue: 1,
// };

// const contentReduser = (state = initialState, action) => {
//   switch (action.type) {
//     case 'INCREASE_VALUE':
//       return { ...state, number: state.number + action.value };

//     case 'DECREASE_VALUE':
//       return { ...state, number: state.number - action.value };

//     default:
//       return state;
//   }
// };

// export const increaseAction = (value) => ({ type: INCREASE_VALUE, value });
// export const decreaseAction = (value) => ({ type: DECREASE_VALUE, value });

// export default contentReduser;
